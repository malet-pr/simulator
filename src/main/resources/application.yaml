server:
    port: 8092
spring:
    application:
        name: simulator
    kafka:
        bootstrap-servers: localhost:9092
management:
    endpoints:
        web:
            exposure:
                include: health, info, prometheus
    tracing:
        enabled: true
